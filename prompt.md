# Complete User Prompt History

Below is the chronological sequence of prompts provided to the AI during the construction of this Next.js Infinite Scroll Architecture:



1. **Prompt 1:** `"Create a custom hook using useInfiniteQuery for fetching books with query and pagination. Include getNextPageParam logic."`
2. **Prompt 2:** 
    `"task1) Create a responsive BookCard component using Tailwind and shadcn Card. Include image, title (2-line clamp), author, year, and subject badges.`
    `task2) Create a responsive grid layout using Tailwind with 1-5 columns depending on screen size for displaying book cards."`
3. **Prompt 3:** `"Implement infinite scroll using IntersectionObserver in React to load next page when bottom sentinel is visible."`
4. **Prompt 4:** 
    `"task1) Implement virtualization using react-virtual for a grid of cards with dynamic heights. Ensure smooth scrolling performance.`
    `task2) Create a debounced search input (400ms) using React and update query state.`
    `go for task 2 once you implemented task1"`
5. **Prompt 5:** 
    `"task1) Create filter chips (science, math, history) that update search query and reset pagination.`
    `task2) Create skeleton loading UI for book cards using shadcn Skeleton component.`
    `go for task2 only if you have implemented task1"`
6. **Prompt 6:** 
    `"task1) Implement dark/light mode toggle using Tailwind and shadcn in Next.js.`
    `task2) Sync search query with URL params (?q=...) and restore state on reload in Next.js app router.`
    `go for task2 only once you have implemented task1"`
7. **Prompt 7:** 
    `"task1) Prefetch next page using React Query when user scroll is near bottom.`
    `task2) Combine search bar, book grid, infinite scroll, and virtualization into a single page using React Query.`
    `go for task 2 only if you have implemented task1"`

8. **Prompt 8:** 
    `"task1) create a read me file `
    `Include:`
    `Setup`
    `Architecture`
    `Virtualization explanation`
    `task2) create a prompt.md file`
    `that include all prompts I had given you"`
